kind = "web"
previewPath = "/"
title = "Pulseify"
version = "1.0.0"
id = "artifacts/pulseify"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19979

[services.development]
run = "pnpm --filter @workspace/pulseify run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pulseify", "run", "build" ]
publicDir = "artifacts/pulseify/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19979"
BASE_PATH = "/"
